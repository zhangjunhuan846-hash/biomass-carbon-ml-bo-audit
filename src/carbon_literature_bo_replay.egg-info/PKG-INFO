Metadata-Version: 2.4
Name: carbon-literature-bo-replay
Version: 1.0.0
Summary: Offline Bayesian optimization replay for literature-derived carbon materials datasets.
Author: materials review audit workflow
License: MIT
Requires-Python: >=3.9
Description-Content-Type: text/markdown
License-File: LICENSE
Requires-Dist: numpy>=1.24
Requires-Dist: pandas>=2.0
Requires-Dist: matplotlib>=3.7
Requires-Dist: pyyaml>=6.0
Requires-Dist: openpyxl>=3.1
Dynamic: license-file

# carbon-literature-bo-replay-skill

<p align="center">
  <b>面向碳材料文献数据库的离线贝叶斯优化回放 Skill</b><br>
  <span>Offline Bayesian Optimization Replay for Literature-Derived Carbon Materials Datasets</span>
</p>

<p align="center">
  <img alt="status" src="https://img.shields.io/badge/status-research--workflow-blue">
  <img alt="version" src="https://img.shields.io/badge/version-v1.0.0-black">
  <img alt="domain" src="https://img.shields.io/badge/domain-AI4Materials-green">
  <img alt="workflow" src="https://img.shields.io/badge/workflow-JSON--first-orange">
  <img alt="agents" src="https://img.shields.io/badge/agents-multi--agent-purple">
</p>

---

## 项目定位

`carbon-literature-bo-replay-skill` 是一个面向 **化工与材料工程文献数据库** 的 Codex-native 多 agent Skill，用于在没有真实实验闭环的情况下，对已发表文献数据进行 **离线贝叶斯优化回放**。

它的核心目标不是声称“自动发现新材料”，而是回答一个更严谨的问题：

> 如果我们把已有文献数据库当作一个离线候选池，贝叶斯优化策略是否比随机搜索、贪婪搜索或多样性采样更快找到高性能碳材料样本？

适用对象包括但不限于：

- 生物质衍生硬碳用于钠离子电池（SIB）
- 生物质 / 多孔碳用于锂离子电池（LIB）
- 水系超级电容器碳材料（aqueous SC）
- 文献提取型材料数据库的模型筛选与候选排序
- AI4Materials / AI for Science 方向的低成本方法学验证

---

## 为什么需要这个 Skill？

材料领域经常出现一种“看起来像闭环优化、实际上只是模型拟合”的工作流：

```text
文献数据库 → 随机森林 / XGBoost → SHAP → 推荐几个候选点
```

这类流程有价值，但容易产生三个问题：

1. **没有验证搜索效率**：模型是否真的比随机搜索更快找到优良样本？
2. **没有区分真实闭环与离线回放**：文献数据库中的目标值已经存在，不能包装成真实实验发现。
3. **没有泄漏审计**：同一论文、同一材料系列、同一测试条件可能同时进入训练池和候选池，导致过高估计性能。

本 Skill 将任务重新定义为更稳健的 **offline BO replay**：

```text
已知文献数据库
      ↓
隐藏一部分样本作为候选池
      ↓
从少量初始样本开始
      ↓
模型每轮推荐一个候选样本
      ↓
揭示该样本在文献中的真实性能
      ↓
比较 BO / random / exploit-only / diversity 的搜索效率
      ↓
输出是否值得进入真实实验闭环的证据
```

---

## 核心功能

### 1. Literature Dataset Profiler

自动读取文献数据库，生成数据画像：

- 样本数量
- 文献数量
- 材料体系分布
- 目标性能分布
- 缺失率
- 可用描述符字段
- 高风险字段

输出：

```text
state/01_dataset_profile.json
```

---

### 2. Descriptor Schema Agent

面向碳材料体系识别常用描述符：

```text
carbonization_temperature
activation_temperature
BET
pore_volume
micropore_volume
d002
ID_IG
XPS_N
XPS_O
mass_loading
electrode_thickness
compacted_density
current_density
voltage_window
```

并标记：

- 可作为模型输入的字段
- 不应作为输入的泄漏字段
- 需要单位统一的字段
- 缺失严重的字段

输出：

```text
state/02_descriptor_schema.json
selected_features.csv
```

---

### 3. Replay Protocol Builder

构建离线回放协议：

- 初始 seed set 大小
- 候选池大小
- 每轮选择数量
- 总迭代轮数
- 是否按文献分组划分
- 是否启用 leave-one-paper-group-out 审计
- 随机基线重复次数

输出：

```text
state/03_replay_protocol.json
```

---

### 4. Surrogate Model Agent

支持轻量 surrogate model：

- Ridge / linear surrogate fallback
- Random Forest surrogate（可选，依赖 scikit-learn）
- Gaussian Process surrogate（可选，依赖 scikit-learn）
- uncertainty proxy
- model diagnostics

输出：

```text
state/04_surrogate_diagnostics.json
```

---

### 5. Acquisition Replay Agent

支持多种 acquisition 策略：

```text
UCB              mean + beta * uncertainty
EI-like          improvement-weighted ranking
exploit-only     predicted mean only
diversity        distance-based diversity sampling
random           random baseline
```

输出：

```text
replay_trace.csv
recommended_candidates.csv
state/05_acquisition_trace.json
```

---

### 6. Baseline Comparison Agent

与多个基线比较：

- random search
- exploit-only
- diversity sampling
- oracle upper bound

核心评价指标：

```text
best-found curve
round-to-top-k
area under best-found curve
final best target
improvement over random baseline
```

输出：

```text
baseline_comparison.csv
state/06_baseline_comparison.json
figures/bo_replay_curve.png
```

---

### 7. Leakage & Validity Audit

这是本项目最关键的科学边界模块之一。它检查：

- 同一论文样本是否同时出现在训练池和候选池
- 同一材料系列是否跨池泄漏
- 目标变量是否被错误作为特征输入
- 派生字段是否包含目标信息
- 高度重复样本是否导致搜索效率虚高
- 数据集是否太小，不适合宣称优化优势

输出：

```text
leakage_audit.csv
state/07_leakage_audit.json
```

---

### 8. Decision Report

最终生成投稿 / 汇报可用的回放报告：

```text
bo_replay_report.md
```

报告会明确区分：

| 判断 | 含义 |
|---|---|
| GO_EXPERIMENT | 离线回放支持进入小规模实验闭环 |
| GO_MORE_DATA | 数据量或覆盖率不足，应先补数据库 |
| METHOD_ONLY | 可作为方法演示，但不足以支持材料结论 |
| STOP | 泄漏、样本量或目标定义问题过大，不建议继续 |

---

## 多 agent + JSON-first 工作流

本项目不是让每个 agent 重复阅读完整 Excel 或全文材料，而是通过 JSON 中间文件传递最小上下文：

```text
raw_literature_database.csv / .xlsx
        ↓
01_dataset_profile_agent
        ↓
state/01_dataset_profile.json
        ↓
02_descriptor_schema_agent
        ↓
state/02_descriptor_schema.json
        ↓
03_replay_protocol_agent
        ↓
state/03_replay_protocol.json
        ↓
04_surrogate_agent
        ↓
state/04_surrogate_diagnostics.json
        ↓
05_acquisition_replay_agent
        ↓
state/05_acquisition_trace.json
        ↓
06_baseline_comparison_agent
        ↓
state/06_baseline_comparison.json
        ↓
07_leakage_validity_agent
        ↓
state/07_leakage_audit.json
        ↓
08_report_agent
        ↓
bo_replay_report.md
```

这样做的优点：

- 降低 token 消耗
- 保留每一步决策证据
- 方便 Codex / OpenClaw / ChatGPT 多 agent 继续接管
- 避免“黑箱式一次性分析”
- 便于后续论文 SI 或 GitHub case study 复现

---

## 输入文件

推荐输入：

```text
inputs/
├── literature_database.xlsx 或 literature_database.csv
├── descriptor_schema.yaml          # 可选
├── replay_config.yaml              # 可选
└── protected_columns.yaml          # 可选，防止泄漏字段进入模型
```

最低要求字段：

| 字段 | 是否必需 | 说明 |
|---|---:|---|
| sample_id | 推荐 | 样本唯一编号 |
| paper_id | 推荐 | 文献编号，用于泄漏审计 |
| material_name | 推荐 | 样品名 |
| target | 必需 | 优化目标，例如 ICE、capacity、capacitance |
| descriptor columns | 必需 | 至少 3 个数值型描述符 |

---

## 输出文件

```text
outputs/
├── bo_replay_report.md             # 主报告
├── replay_trace.csv                # 每轮 BO 回放轨迹
├── recommended_candidates.csv      # 推荐候选样本清单
├── baseline_comparison.csv         # 与随机/贪婪/多样性基线比较
├── leakage_audit.csv               # 数据泄漏与有效性审计
├── selected_features.csv           # 最终进入模型的描述符
├── state/
│   ├── 01_dataset_profile.json
│   ├── 02_descriptor_schema.json
│   ├── 03_replay_protocol.json
│   ├── 04_surrogate_diagnostics.json
│   ├── 05_acquisition_trace.json
│   ├── 06_baseline_comparison.json
│   └── 07_leakage_audit.json
└── figures/
    └── bo_replay_curve.png
```

---

## 安装

```bash
pip install -r requirements.txt
pip install -e .
```

---

## 快速运行

使用内置 demo：

```bash
python examples/run_demo.py
```

或使用命令行：

```bash
python -m carbon_literature_bo_replay.cli replay \
  --data examples/input/carbon_literature_demo.csv \
  --target ICE \
  --id-col sample_id \
  --paper-col paper_id \
  --out outputs/demo_replay \
  --seed-size 8 \
  --iterations 20 \
  --random-repeats 50
```

---

## 适合你的研究场景

如果你的数据库来自生物质储能碳材料综述，例如：

```text
LIB 所有样本.xlsx
SIB 所有样本.xlsx
水系超级电容器样本.xlsx
```

可以分别设置目标：

| 体系 | 推荐 target | 注意事项 |
|---|---|---|
| SIB | ICE / reversible capacity / plateau capacity | 电解液体系需分层，避免碳酸酯/醚系混合 |
| LIB | ICE / reversible capacity / rate capability | 测试电流密度和电压窗口需严格记录 |
| 水系 SC | specific capacitance / retention | 三电极与两电极不能直接混合 |

建议先进行：

```text
ai-extracted-data-cleaner
        ↓
cleaned_database.xlsx
        ↓
carbon-literature-bo-replay-skill
        ↓
materials-review-audit-skill
```

---

## 科学边界声明

本 Skill 做的是 **offline literature replay**，不是完整的 autonomous laboratory，也不等同于真实实验闭环。

它可以支持的表述：

```text
The offline replay suggests that the acquisition strategy identifies high-performing literature samples more efficiently than random search under the defined descriptor space and replay protocol.
```

它不应该支持的表述：

```text
The model discovered a new optimal carbon material.
```

更稳健的中文表述是：

```text
离线文献回放结果提示，在当前描述符空间和候选池定义下，贝叶斯优化策略相比随机搜索更快定位到已报道的高性能样本；该结果可作为后续小规模实验闭环设计的依据，而不应被解释为新材料发现。
```

---

## 仓库结构

```text
carbon-literature-bo-replay-skill/
├── README.md
├── README_en.md
├── skill.md
├── AGENTS.md
├── requirements.txt
├── pyproject.toml
├── config/
│   ├── descriptor_rules.yaml
│   ├── replay_defaults.yaml
│   └── leakage_rules.yaml
├── prompts/
│   ├── 01_dataset_profile_agent.md
│   ├── 02_descriptor_schema_agent.md
│   ├── 03_replay_protocol_agent.md
│   ├── 04_surrogate_agent.md
│   ├── 05_acquisition_replay_agent.md
│   ├── 06_baseline_comparison_agent.md
│   ├── 07_leakage_validity_agent.md
│   └── 08_report_agent.md
├── schemas/
├── src/carbon_literature_bo_replay/
├── examples/
├── tests/
└── docs/
```

---

## 与其他 Skill 的关系

```text
ai-extracted-data-cleaner
        ↓
清洗 AI/OCR/PDF 提取数据，生成二次核查队列
        ↓
carbon-literature-bo-replay-skill
        ↓
用文献数据库进行离线 BO 回放，评估搜索策略是否优于随机
        ↓
materials-review-audit-skill
        ↓
审计综述正文、图表、source_data、数据库和引用证据的一致性
```

这三个项目组合起来，可以形成一个完整的 AI4Materials 研究工作流：

```text
文献数据抽取 → 数据清洗 → 离线优化回放 → 综述审计 → 实验闭环设计
```

---

## 适合放在 GitHub 首页的一句话

```text
一个面向碳材料文献数据库的离线贝叶斯优化回放 Skill，用于在真实实验闭环之前，审计数据可用性、验证搜索策略效率，并生成可复现的候选筛选报告。
```

---

## License

MIT License

